import { useLoaderData } from "react-router-dom"

const Categories = () => {
    const data = useLoaderData()
    console.log(data)
  return (
    <div>
      
    </div>
  )
}

export default Categories
